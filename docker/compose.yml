services:
  frontend:
    build:
      context: .
      dockerfile: Netflix
    ports:
      - "8200:80"

  backend:
    build:
      context: .
      dockerfile: tomcat
    ports:
      - "8500:8080"
